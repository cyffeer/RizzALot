# Server environment variables

PORT=5000

# Either MONGODB_URI or MONGO_URI:
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/rizz_a_lot?retryWrites=true&w=majority
# Optional override if your URI has no db name:
# DB_NAME=rizz_a_lot

# Use a strong secret in production
JWT_SECRET=change-me

# CORS allowed origin for the frontend
CLIENT_ORIGIN=http://localhost:5173

# File uploads (optional; set when using Render Persistent Disk)
# UPLOADS_DIR=/var/data/uploads

# AI (optional)
# GEMINI_API_KEY=your-key
# GEMINI_MODEL=models/gemini-1.5-flash-8b-latest