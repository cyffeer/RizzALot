name: Keep Render Warm
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health
        run: curl -fsS https://rizzalot.onrender.com/api/health || true